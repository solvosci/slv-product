<odoo>

    <record id="product_pricelist_item_form_view_inherit" model="ir.ui.view">
        <field name="name">product.pricelist.item.form.inherit</field>
        <field name="model">product.pricelist.item</field>
        <field name="inherit_id" ref="product.product_pricelist_item_form_view"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='base']" position="after">
                <field name="coefficient" invisible="base != 'supplierinfo'"/>
            </xpath>
            <xpath expr="//field[@name='base_pricelist_id']" position="after">
                <field name="show_coefficient" invisible="1"/>
            </xpath>
            <xpath expr="//group[@name='pricelist_rule_base']//div[contains(@class, 'alert alert-info')]" position="inside">
                <div invisible="not show_coefficient or compute_price != 'formula' or base != 'pricelist'">
                    <strong>Rule other price list: <field name="base_pricelist_id"/></strong><br/>
                    <span>Base on: <field name="base_from_pricelist"/></span><br/>
                    <span>Coefficient: <field name="coefficient"/></span><br/>
                    <span>Discount: <field name="price_discount_supplierinfo"/></span><br/>
                </div>
            </xpath>
        </field>
    </record>

    <record id="product_pricelist_item_tree_view_inherit" model="ir.ui.view">
        <field name="name">product.pricelist.item.tree.inherit</field>
        <field name="model">product.pricelist.item</field>
        <field name="inherit_id" ref="product.product_pricelist_item_tree_view"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='price']" position="before">
                <field name="base" column_invisible="1"/>
                <field name="coefficient" invisible="base != 'supplierinfo'"/>
            </xpath>
        </field>
    </record>

</odoo>
