<odoo>

    <record id="product_pricelist_form_view_inherit" model="ir.ui.view">
        <field name="name">product.pricelist.form.inherit</field>
        <field name="model">product.pricelist</field>
        <field name="inherit_id" ref="product.product_pricelist_view"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='price']" position="before">
                <field name="coefficient"/>
                <field name="coefficient_from_pricelist" optional="hide"/>
                <field name="price_discount_supplierinfo" optional="hide"/>
            </xpath>
        </field>
    </record>

</odoo>
